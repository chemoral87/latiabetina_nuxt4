# church-member-consolidator-assign

Archivos que referencian el permiso `church-member-consolidator-assign`.

## Files

- `app\pages\church-member\[id]\index.vue` (visibilidad de la sección/acción de asignación de consolidador)

## Also referenced in

- `app\utils\permissionChipColor.ts` y `app\utils\permissionChipColor.test.ts` (solo como dato de prueba/ejemplo para orden y colores de chips, no como control de acceso)
