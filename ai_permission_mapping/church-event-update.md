# church-event-update

Archivos que referencian el permiso `church-event-update`.

## Files

- `app\pages\church-event\[id]\index.vue` (prop `permission="church-event-update"` sobre `ChurchEventForm`; nota: el meta `permission` de esta página es `church-event-index`, por lo que la ruta solo exige `church-event-index`)
