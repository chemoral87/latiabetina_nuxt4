# conso-sheet-consolidator-select

Archivos que referencian el permiso `conso-sheet-consolidator-select`.

## Files

- `app\pages\consolidation\[id]\details.vue` (oculta el campo `det-consolidator-ac` si no se tiene el permiso)

## Also referenced in

- `app\components\Consolidation\ConsolidatorCombobox.vue` (combobox de usuarios consolidadores; la búsqueda usa `ConsoSheet.consolidators`, que requiere `conso-sheet-index` en la API)