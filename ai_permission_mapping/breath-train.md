# breath-train

Archivos que referencian el permiso `breath-train`.

## Files

- `app\pages\relax\index.vue` (únicamente como comentario; el meta `permission: "breath-train"` está comentado y la página no exige el permiso)

## Also referenced in

- `app\services\menu-service.ts` (visibilidad en el menú)