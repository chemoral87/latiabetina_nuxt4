# song-update

Archivos que referencian el permiso `song-update`.

## Files

- `app\pages\song\[id]\edit.vue` (meta `permission` y prop `permission="song-update"` sobre `SongEditor`)
- `app\components\Song\Editor.vue` (declara la prop opcional `permission?: string` pero actualmente no la utiliza)
