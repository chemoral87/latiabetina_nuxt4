# church-event-create

Archivos que referencian el permiso `church-event-create`.

## Files

- `app\pages\church-event\new.vue` (prop `permission="church-event-create"` sobre `ChurchEventForm`; nota: el meta `permission` de esta página es `church-event-index`, por lo que la ruta solo exige `church-event-index`)
